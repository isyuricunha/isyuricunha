name: update readme

on:
  push:
    branches: master
  schedule:
    - cron: "*/15 * * * *" # 15 minutes update quote/link/info

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]

    steps:
      - uses: actions/checkout@v3

      - name: config python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: readme update
        run: python update_readme.py

        steps:
      - name: "Checkout repository"
        uses: actions/checkout@v2
      
      - name: "Build & test"
        run: |
          echo "done!"

      - name: "Get latest tag"
        id: latest_tag
        run: echo "::set-output name=tag::$(git describe --abbrev=0 --tags)"
          
      - name: "Generate changelog and create release"
        uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "v${{ steps.parse_tag.outputs.version }}"
          prerelease: false
          title: "Release v${{ steps.parse_tag.outputs.version }}"
          files: |
            *

      - name: commit readme
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          git add README.md
          git commit -am "update info/link/quote, idk"
          git push --all -f https://${{ secrets.GITHUB_TOKEN }}@github.com/${GITHUB_REPOSITORY}.git
